<?= $this->partial(
    'db-etree-org/partial/workspace',
    array('entity' => $this->performerAlias)
);
?>

<div class="h1">

    <a class="btn btn-info" href="<?=
        $this->url('performer/detail', array(
            'id' => $this->performerAlias->getPerformer()->getId(),
        )); ?>">
        <i class="icon-film"></i>
    </a>

    <?= $this->escapeHtml($this->performerAlias->getPerformer()->getName()); ?>
</div>

<div class="h1">
    <a data-id="<?= $this->performerAlias->getId(); ?>" class="btn btn-warning edit-performer-alias" href="#">
        <i class="icon-github-alt"></i>
    </a>

    <?= $this->escapeHtml($this->performerAlias->getName()); ?>
</div>

<?php
if ($this->escapeHtml($this->performerAlias->getNote())) {
?>
    <pre><?= $this->escapeHtml($this->performerAlias->getNote()); ?></pre>
<?php
}
?>
